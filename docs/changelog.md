<!--
  Single source of truth: this page embeds the canonical CHANGELOG.md from the
  repository root (generated by `make changelog` via git-cliff). Do not maintain
  a separate changelog here — edit the root CHANGELOG.md instead.
-->
--8<-- "CHANGELOG.md"
